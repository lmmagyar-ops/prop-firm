# Nixpacks configuration for Railway
# Specifies Node.js 20 to meet package requirements

[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x", "openssl"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm install"]

[start]
cmd = "npx tsx src/workers/ingestion.ts"
